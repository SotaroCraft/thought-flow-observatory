# M5 smoke configuration

Smoke-only, versioned artifacts for Milestone M5.

- `PROVISIONAL-M5-SMOKE` is **not** Gate D / production dictionary.
- Do not rename these files to a production dictionary path.
- Vocabulary edits require an explicit smoke revision (`rN`); do not silently expand terms.
